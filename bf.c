#include <stdio.h>
#include <stdint.h>
#include <stdlib.h>

#define CELL uint8_t

int main(int argc, char** argv) {
    setbuf(stdout, NULL);
    setbuf(stderr, NULL);

    if(argc != 2) {
        //
    }
}